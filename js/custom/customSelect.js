$(document).ready(function($) {
	$( "#Brasil" ).draggable({ snap: ".ui-widget-header" });
	$( "#Croacia" ).draggable({ snap: ".ui-widget-header" });
	$( "#Mexico" ).draggable({ snap: ".ui-widget-header" });
	$( "#Camerun" ).draggable({ snap: ".ui-widget-header" });
});